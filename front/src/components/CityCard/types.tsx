export type CityProps = {
  name: string;
  image: string;
};
export type City = {
  name: string;
  image: string;
  id: string;
  description: string;
  placesOfInterest: string[];
  images: string[];
};
